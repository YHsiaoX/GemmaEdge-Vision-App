name: Synapse
targets:
  Synapse:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources: [Sources]
    settings:
      INFOPLIST_FILE: Info.plist
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
      GENERATE_INFOPLIST_FILE: NO
      CODE_SIGNING_ALLOWED: NO
      CODE_SIGNING_REQUIRED: NO
schemes:
  Synapse:
    shared: true  # 核心修复：强制云端暴露此 Scheme
    build:
      targets:
        Synapse: all
